# Design-Paket Migration 274 — `documents`-INSERT-Mandantentrennung (3-Policy-Verbund)

**Datum:** 2026-07-11 (früh) · **Status:** DESIGN (von Ben beauftragt; Umsetzung als eigener Schritt) · **Basis:** angewandte Migration 273, Audit `2026-07_rls_restrunde_audit.md` Punkt 2.

## 1. Problem

INSERT auf `documents` ist erlaubt, wenn IRGENDEINE permissive Policy-WITH-CHECK passt:

| Policy | WITH CHECK (Ist) |
|---|---|
| `documents_insert` (INSERT) | **`true`** — die offene Scheunentür |
| `documents_company_isolation` (ALL) | company-match ∨ `app.is_admin` |
| `documents_tenant_isolation` (ALL) | bypass ∨ **`company_id IS NULL`** ∨ company-match |

Folge: eine kontextlose App-Rollen-Session kann beliebige (auch fremd-company- oder NULL-company-) Dokumente EINFÜGEN, obwohl sie seit 272/273 nichts mehr LESEN kann.

## 2. Empirie (2026-07-11, live gemessen)

1. **0 Dokumente mit `company_id IS NULL`** im Bestand (4 Docs, alle mit company_id; 1 ownerlos = Spiegel).
2. **Privat-Suite ist NICHT betroffen**: sie schreibt in ihre eigene Tabelle `PrivatDocument` (`app/services/privat/document_service.py:495`), nicht in `documents` — der NULL-Escape ist für Privat NICHT tragend. (Einziger `documents`-Bezug in privat/: ein READ in `tax_assistant_service.py:285`.)
3. Alle kanonischen Ersteller setzen company_id: Import-Pfad (`imports/document_creation.py:224`), Mirror (`odoo_mirror_service.py:453` = connection.company_id), API-Upload (require_company-Kontext).

→ Es gibt KEINEN legitimen Produzenten von NULL-company-`documents`. Der NULL-Zweig kann ersatzlos entfallen (kein owner-gebundener Sonderpfad nötig).

## 3. Ziel-Design (Migration 274)

1. **`DROP POLICY documents_insert`** — ersatzlos; die beiden ALL-Policies decken INSERT vollständig und korrekt. (Downgrade: `CREATE POLICY documents_insert ON documents FOR INSERT WITH CHECK (true)`.)
2. **`documents_tenant_isolation` WITH CHECK** → `is_rls_bypass_enabled() OR company_id = get_current_company_id()` (NULL-Zweig raus; USING wurde bereits in 272 gehärtet — ALTER POLICY … WITH CHECK (…) ohne USING-Angabe lässt USING unberührt).
3. `documents_company_isolation` bleibt unverändert (company ∨ admin).

**Resultierende INSERT-Matrix:** Worker-Bypass ✓ · Company-Kontext (nur eigene company_id) ✓ · Admin ✓ · kontextlos ✗ · NULL-company ✗ · fremde company ✗. UPDATE-new-row unterliegt denselben WITH CHECKs (gewollt).

## 4. ⚠️ Vorbedingung: kontextlose Writer umstellen (der eigentliche Arbeitsanteil)

Nach 274 scheitert jeder `documents`-INSERT aus einer Session OHNE Bypass/Company-Kontext. Inventar der `get_async_session_context`-Nutzer (Grep 2026-07-11) mit Schreibverdacht — je Fundstelle klären: schreibt sie `documents`? → auf `get_worker_session_context` (Bypass) bzw. Company-Kontext umstellen:

| Fundstelle | Verdacht | Maßnahme |
|---|---|---|
| **`scripts/import_wa_we.py:448`** | **SICHER betroffen** — legt `Document`-Zeilen an; würde mitten in Go-Live-Block W abgelehnt! | auf `get_worker_session_context()` umstellen (systemischer Import, analog import_tasks) — **MUSS vor 274-Anwendung, idealerweise vor 01.08.** |
| `app/agents/orchestration/ocr_router.py:1059` | prüfen (schreibt vermutlich Document-Status) | Update ≠ INSERT: UPDATE braucht USING+CHECK — ebenfalls umstellen falls documents-Writes |
| `app/workers/contract_deadline_checker.py:91` | prüfen | dito |
| `app/workers/tasks/active_learning_tasks.py:78,193` | prüfen | dito |
| `app/db/session.py:216f` (`async_session_factory`/`_maker`-Aliase!) | **Transitiv**: alle Nutzer dieser Aliase erben Kontextlosigkeit | Alias-Nutzer inventarisieren (eigener Grep) |

## 5. Testkonzept (vor GATE)

1. **Integrationstest-Matrix** (Erweiterung `test_rls_worker_context.py`, echte RLS-Rolle): INSERT via Bypass ✓ / via Company-Kontext eigene ✓ / fremde company ✗ / kontextlos ✗ / NULL-company mit Kontext ✗.
2. **Privat-Flusstest** (Beweis der Nicht-Betroffenheit): Privat-Upload end-to-end über die API → `PrivatDocument`-Zeile entsteht, kein `documents`-INSERT im Pfad (SQL-Assertion vorher/nachher).
3. **WA/WE-Regressionstest**: `import_wa_we.py --execute --limit 1` gegen Test-DB NACH Umstellung auf Worker-Kontext.
4. **Pipeline-Smoke live**: Upload → OCR → completed (deckt ocr_router-Statuswrites).

## 6. Umsetzungsreihenfolge (frische Session)

① Writer-Inventar §4 abarbeiten (TDD je Umstellung) → ② Migration 274 nach §3 (272er-Muster inkl. downgrade, ROLLBACK-Probelauf) → ③ Testmatrix §5 grün → ④ GATE (Anwendung = nächster Backend-Neustart/`upgrade head`) → ⑤ Live-DoD: kontextloser INSERT abgelehnt, Mirror-Lauf + Upload + WA/WE-Probelauf grün.

**Aufwand:** ~1,5–2,5 PT (dominiert von §4-Inventar + Tests, nicht von der Migration).

---

## 7. UMSETZUNGSSTAND (2026-07-11, gleiche Session, GATE von Ben erteilt)

✅ **KOMPLETT.** §4-Writer umgestellt: `import_wa_we` → `get_worker_session_context` (der alte transaktions-lokale Bypass verdampfte beim Commit-pro-Datei nach Datei 1 — latenter Dedupe-/Duplikat-Bug schon VOR 274, jetzt zu; Re-Arm-Code entfernt; Quelltext-Wache-Test) und `active_learning_tasks` (2 Stellen; sah post-273 still 0 companies). `ocr_router:1062`/`contract_deadline_checker:91` = reine READS (kein documents-Write, keine 274-Vorbedingung; Silent-0-Rows-Caveat notiert). **Migration 274 ANGEWANDT** (273→274, `alembic current` = 274 head) — INSERT-Matrix-Integrationstest lief scharf: **5 passed** (kontextlos abgelehnt mit row-level-security-Fehler, NULL-company abgelehnt, Worker-Bypass + Company-Kontext erlaubt). Damit ist die RLS-Restrunde (Audit-Punkte 1–4 + dieser Verbund) geschlossen; offen bleibt nur das `async_session_factory`-Alias-Inventar als Hygiene-Folge (Aliase in `session.py:216f`, Nutzer bei Gelegenheit inventarisieren).
