---
description: "Start Ralph Loop in current session"
argument-hint: "PROMPT [--max-iterations N] [--completion-promise TEXT]"
allowed-tools: ["Bash(setup-ralph-loop.sh:*)"]
---

Invoke the ralph-loop plugin:

Use the Skill tool with skill="ralph-loop:ralph-loop" and pass the user's arguments.
