# pg_hba.conf - SSL Enforcement for Ablage-System
# Generated: Security Hardening Phase 1
#
# TYPE  DATABASE  USER  ADDRESS       METHOD
#
# Local connections via Unix Socket (no SSL needed)
local   all       all                 peer
#
# IPv4 SSL-only connections (SCRAM-SHA-256)
hostssl all       all   0.0.0.0/0     scram-sha-256
#
# IPv6 SSL-only connections (SCRAM-SHA-256)
hostssl all       all   ::/0          scram-sha-256
#
# IMPORTANT: No 'host' entries = non-SSL connections are rejected
